
<% if user_signed_in? %>
    <%= User.where(email: current_user.email).first.inspect %>
    <%= link_to('Logout', destroy_user_session_path, :method => :delete) %>
<% else %>
    <%= User.all.inspect %>
<% end %>